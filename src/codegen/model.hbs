{{#each imports}}
import { {{name}} } from '{{path}}';
{{/each}}
export class {{name}} {

{{#each properties}}
    {{name}}!: {{model.name}};
{{/each}}

    constructor(raw: {{name}}) {
{{#each properties}}
    {{#if isClassModel}}
        this.{{name}} = new {{model.name}}(raw.{{name}});
    {{else}}
        this.{{name}} = raw.{{name}};
    {{/if}}
{{/each}}
    }
}